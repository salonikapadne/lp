## port server=5000
## port client =3000

# SECURITY GROUP
# OPEN: 22,cutom tcp{3000, 5000, 5173}

sudo apt update -y && sudo apt upgrade -y
sudo apt install git -y

# NODE
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install nodejs -y

# PM2
sudo npm install -g pm2

# MONGODB
sudo apt install gnupg curl -y

curl -fsSL https://pgp.mongodb.com/server-7.0.asc | \
sudo gpg -o /usr/share/keyrings/mongodb-server-7.0.gpg \
--dearmor

echo "deb [ signed-by=/usr/share/keyrings/mongodb-server-7.0.gpg ] \
https://repo.mongodb.org/apt/ubuntu jammy/mongodb-org/7.0 multiverse" | \
sudo tee /etc/apt/sources.list.d/mongodb-org-7.0.list

sudo apt update -y
sudo apt install mongodb-org -y

sudo systemctl start mongod
sudo systemctl enable mongod

# CLONE
git clone https://github.com/salonikapadne/to-do-app.git
cd to-do-app

# BACKEND
cd server
npm install
npm install dotenv

echo "PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/todoapp" > .env

# IMPORTANT:
# require('dotenv').config();

pm2 start index.js --name backend

# FRONTEND
cd ../client
npm install

# CHECK AVAILABLE SCRIPTS
npm run

# IF START EXISTS:
pm2 start npm --name frontend -- start

# IF DEV EXISTS INSTEAD:
# pm2 start "npm run dev -- --host" --name frontend

pm2 save
pm2 status

-----

cd to-do-app

client env:
//nano .env
PORT=3000
REACT_APP_API_BASE_URL=http://44.226.17.220:5000/api

server env:
//nano .env
PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/todoapp
JWT_SECRET=my_secret_key


sudo pm2 list

check http://44.226.17.220:3000
