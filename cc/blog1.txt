sudo apt update
sudo apt install -y nginx curl git build-essential

curl -fsSL https://www.mongodb.org/static/pgp/server-8.0.asc | sudo gpg -o /usr/share/keyrings/mongodb-server-8.0.gpg --dearmor
echo "deb [ arch=amd64,arm64 signed-by=/usr/share/keyrings/mongodb-server-8.0.gpg ] https://repo.mongodb.org/apt/ubuntu noble/mongodb-org/8.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-8.0.list
sudo apt update
sudo apt install -y mongodb-org
sudo systemctl daemon-reload
sudo systemctl enable mongod
sudo systemctl start mongod
sudo systemctl status mongod

curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.4/install.sh | bash
. "$HOME/.nvm/nvm.sh"
nvm install 24
npm install -g pm2

cd /home/ubuntu
git clone http://github.com/salonikapadne/blog1.git
cd blog1

cd backend
npm install
cd ../frontend
npm install

cd backend
nano .env
PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/mern-blog


cd ../frontend
nano .env
VITE_API_URL=http://YOUR_PUBLIC_IP:5000/api/blogs

cd /home/ubuntu/blog1/frontend
npm run build

cd /home/ubuntu/blog1/backend
sudo pm2 start server.js --name blog-backend

cd /home/ubuntu/blog1/frontend
sudo pm2 serve dist 3000 --name blog-frontend --spa

sudo pm2 save
sudo pm2 startup systemd -u ubuntu --hp /home/ubuntu