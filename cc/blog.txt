# APP ENVIRONMENT VARIABLES (backend)
PORT=8000
MONGODB_URL=mongodb+srv://berthutapea:zkZ0tg0H7yiScvO5@cluster0.jwcfbn8.mongodb.net/db_student?retryWrites=true&w=majority

# frontend .env
PORT=4000
=http://52.10.173.52:8000


add security rules:
custom tcp-8000
custom tcp-4000


-------
sudo apt update -y
sudo apt install git -y
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install nodejs -y

##verify
node -v
npm -v

sudo npm install -g pm2

cd /home/ubuntu
git clone https://github.com/salonikapadne/blog.git
cd blog
REACT_APP_API
cd Backend 
nano .env
npm install
npm run dev

cd Frontend
npm install
nano .env 
npm start


sudo pm2 start "HOST=0.0.0.0 PORT=4000 npm start" --name frontend
sudo pm2 save
sudo pm2 startup systemd -u ubuntu --hp /home/ubuntu

cd ~/blog/Backend
sudo pm2 start "npm run dev" --name backend
sudo pm2 save 
sudo pm2 startup systemd -u ubuntu --hp /home/ubuntu

now open http://52.10.173.52:4000

